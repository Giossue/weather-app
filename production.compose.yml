services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL in environment variables}
      OPENWEATHER_API_KEY: ${OPENWEATHER_API_KEY:?Set OPENWEATHER_API_KEY in environment variables}
      OPENWEATHER_BASE_URL: ${OPENWEATHER_BASE_URL:-https://api.openweathermap.org}
      CORS_ORIGIN: ${CORS_ORIGIN:?Set CORS_ORIGIN to your public app URL, e.g. https://weather.example.com}
      WEATHER_CACHE_TTL_SECONDS: ${WEATHER_CACHE_TTL_SECONDS:-1800}
      RATE_LIMIT_WINDOW_SECONDS: ${RATE_LIMIT_WINDOW_SECONDS:-60}
      RATE_LIMIT_MAX_REQUESTS: ${RATE_LIMIT_MAX_REQUESTS:-60}
      DEFAULT_LANGUAGE: ${DEFAULT_LANGUAGE:-es}
      DEFAULT_UNITS: ${DEFAULT_UNITS:-metric}
    expose:
      - "3001"
    networks:
      - dokploy-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://localhost:3001/health >/dev/null 2>&1 || exit 1",
        ]
      interval: 30s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    expose:
      - "80"
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.services.cielovivo.loadbalancer.server.port=80"
      - "traefik.http.routers.cielovivo-http.rule=Host(`${APP_HOST:-weather.giossue.com}`)"
      - "traefik.http.routers.cielovivo-http.entrypoints=web"
      - "traefik.http.routers.cielovivo-http.service=cielovivo"
      - "traefik.http.routers.cielovivo-https.rule=Host(`${APP_HOST:-weather.giossue.com}`)"
      - "traefik.http.routers.cielovivo-https.entrypoints=websecure"
      - "traefik.http.routers.cielovivo-https.tls=true"
      - "traefik.http.routers.cielovivo-https.tls.certresolver=letsencrypt"
      - "traefik.http.routers.cielovivo-https.service=cielovivo"
    healthcheck:
      test:
        ["CMD-SHELL", "wget -qO- http://localhost/ >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5

networks:
  dokploy-network:
    external: true
